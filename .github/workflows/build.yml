name: build
on: [push]
jobs:
  build-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
          with:
            python-version: '3.7' 
            architecture: 'x64' 
      - run: python --version